import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.skillcraft.academy',
  appName: 'SkillCraft Academy',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;